# MarkupTagDelimiter

> **IBSheet7 개발자 가이드** > IBSheet Config > MarkupTagDelimiter

---

서버모듈 연동시 전송되는 전문의 마크업 태그의 구분문자를 설정한다. 설정값은 해당 구분문자를 구분자 ","로 연결한 4개의 문자(또는 문자열)로 설정한다.

기본값과 다르게 설정한 경우 서버단 수행로직에서도 동일한 값으로 각각 설정하여야 한다.

(Default="<,>,</,>")